# fly.toml app configuration file generated for orchestrator-hello-world-test on 2026-05-07T19:46:50-06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'orchestrator-hello-world-test'
primary_region = 'iad'

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

# uncomment to enable support for runtime environment variables (also configure Dockerfile and docker-entrypoint.js)
# [[http_service.checks]]
#   grace_period = "30s"   # how long after start before checks begin
#   interval = "10s"
#   timeout = "5s"
#   method = "GET"
#   path = "/"

[[vm]]
  cpus = 1
  memory_mb = 256

[env]
  NEXT_PUBLIC_SUPABASE_URL="https://mqrdpgjavmmklsimnqcx.supabase.co"
  NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_l17M35WHcRV0Z0e0cg6V-Q_0HUjNHZn"
